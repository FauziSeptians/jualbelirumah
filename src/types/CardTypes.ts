export type CardTypes = {
  Images: string;
  Title: string;
  Location: string;
  Clicked?: boolean;
};

export type CardPopularTypes = {
  Images: string;
  Title: string;
  Location: string;
  Harga: number;
  Description: string;
};

export type MiniCardPopularTypes = {
  Images: string;
  Title: string;
  Location: string;
};
